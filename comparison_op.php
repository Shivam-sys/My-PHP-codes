<?php
    $x = 10;
    $y = "10";
    if($x == $y)
        echo "$x and $y are having equal values.";
    else echo "Not equal";
    if($x===$y)
        echo "$x and $y are identical(both values and data type are same.";
    else echo "<br>$x and $y are non identical.(having diffrent values or data type).";
?>